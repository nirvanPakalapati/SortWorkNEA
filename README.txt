To start the program, run quickstart.py which is in the same directory

Before running the program, run "pip --upgrade" to get the latest version of the pip installer if not already installed
Then run "pip install pygame" for the library used for the GUI
